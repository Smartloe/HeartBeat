# HeartBeat 项目环境变量示例
# 复制此文件为 .env 并根据需要修改

# ==================== 后端配置 ====================

# TuneHub API 基础地址
TUNEHUB_BASE_URL=https://music-dl.sayqz.com

# SQLite 数据库路径
TUNEHUB_DB_PATH=backend/app/tunehub.sqlite

# 反爬虫配置（可选，在代码中也有默认值）
# RATE_LIMIT_REQUESTS=60      # 每分钟请求数
# RATE_LIMIT_WINDOW=60        # 时间窗口（秒）
# MAX_LOGIN_ATTEMPTS=5        # 最大登录尝试次数
# LOCKOUT_TIME=300            # 锁定时间（秒）

# 后端端口（启动时指定，这里仅为参考）
# BACKEND_PORT=8000

# ==================== 前端配置 ====================

# 前端环境变量（在 frontend/.env.development 中配置）
# VITE_API_BASE=http://localhost:8000

# ==================== 开发配置 ====================

# 调试模式
DEBUG=true

# 日志级别
LOG_LEVEL=INFO

# ==================== 安全配置 ====================

# 生产环境建议设置
# SECRET_KEY=your-secret-key-here

# ==================== 数据库配置 ====================

# 如果需要使用其他数据库，可以配置连接字符串
# DATABASE_URL=sqlite:///backend/app/tunehub.sqlite
# DATABASE_URL=postgresql://user:password@localhost:5432/heartbeat
# DATABASE_URL=mysql://user:password@localhost:3306/heartbeat

# ==================== 使用说明 ====================

# 1. 后端使用：
#   在启动后端前设置环境变量：
#   export TUNEHUB_BASE_URL=https://music-dl.sayqz.com
#   export TUNEHUB_DB_PATH=backend/app/tunehub.sqlite

# 2. 前端使用：
#   复制 frontend/.env.example 为 frontend/.env.development
#   并设置 VITE_API_BASE 为后端地址

# 3. 生产环境：
#   建议使用 .env.production 文件
#   并设置合适的数据库和安全配置